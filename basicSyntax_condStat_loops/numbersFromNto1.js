function reverseToOne1(num) {

    let i = num;

    while (i >= 1) {
        console.log(i);
        i--
    }
}

function reverseToOne(num) {
    for (let i = num; i >= 1; i--) {
        console.log(i);
    }
}
reverseToOne(5)
reverseToOne(3)